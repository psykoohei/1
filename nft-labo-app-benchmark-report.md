# nft-labo.tokyo スマホアプリ化に向けた現状調査・ベンチマーク分析と Codex 用プロンプト設計レポート

## エグゼクティブサマリ

本レポートは、https://nft-labo.tokyo の現行サイトを「スマホアプリ（iOS/Android想定）」として再構築するために必要な要件を、(1)現状サイト分析、(2)CoinPostベンチマーク、(3)アプリ/バックエンド/ブロックチェーン連携要件、(4)UI/UX設計指針、(5)Codex（OpenAI Codex）へ渡す実装プロンプト設計、の観点で整理する。

- nft-labo.tokyo はニュース/解説記事中心、カテゴリ導線と記事詳細（本文、カテゴリ・タグ、関連記事、埋め込み投稿）で構成。
- 問い合わせフォーム、プライバシーポリシー、会社概要ページが存在。
- コメント領域は「No comments」表示だが、会員機能/サイト内検索/公式アプリ機能は現状未確認。
- CoinPost は検索、チャート、コメント、広告導線、公式アプリ（通知・MyCoin・ダークモード等）が強い。
- 推奨構成は「コンテンツ取得層 + アプリ向けBFF」の二層。将来の認証/通知/コメント/広告/ウォレット連携を見据える。

## 詳細分析

### 1. nft-labo.tokyo 現行機能・構成

- トップにカテゴリ（最新ニュース、BTC、NFT、AI ほか）が並ぶコンテンツ型メディア。
- 記事詳細は本文・著者・カテゴリ/タグ・関連記事を含む。
- 固定ページとして「お問い合わせ」「プライバシーポリシー」「会社概要」。
- プライバシーポリシーはCookie利用・免責を含むが、アプリ向けSDK/広告ID要件は別途整理が必要。
- `/wp-json/` が JSON 応答するため、WordPress REST API 活用余地がある。

### 2. CoinPost ベンチマーク

- モバイル導線が明確（新着、検索、市況/チャート、トピック）。
- コメント機能にログイン・規約・運用ポリシーが組み込まれている。
- 広告/PR/プレスリリースの商用導線が明確。
- 公式アプリでは通知、銘柄追跡、ダークモード、広告表示を提供。

### 3. アプリ要件（方式選定）

推奨：**React Native + TypeScript**（iOS/Android 同時対応）

MVP機能：
1. 記事一覧・記事詳細
2. カテゴリ/タグ
3. 検索（初期はタイトル・タグ）
4. ブックマーク
5. 設定（通知ON/OFF、ダークモード、文字サイズ）

追加方針：
- オフライン対応（直近一覧 + 既読記事キャッシュ）
- プッシュ通知（新着/重要/カテゴリ）
- 将来の会員機能・コメント・課金・ウォレット連携を段階導入

### 4. ブロックチェーン連携方針

- MVPは閲覧中心（非カストディ、秘密鍵をアプリ保持しない）。
- 将来拡張として EVM系 NFT 標準（ERC-721/1155）を想定。
- ログインは OAuth 2.0 + PKCE を基本に、将来 SIWE（EIP-4361）拡張を検討。

### 5. バックエンド設計

推奨：**BFF API**

- 記事取得（CMS連携）
- ブックマーク同期
- 通知デバイストークン管理
- 認証（短命JWT + Refresh）
- レート制限、入力検証、PIIログ抑止

## Codex 実装用プロンプト（再利用版）

### A. プロトタイプ生成

```text
React Native + TypeScript でニュースアプリMVPを実装。
画面: Home/Category/Search/Bookmarks/Settings と Article Detail。
Repository 層でデータ取得抽象化、モックJSONから開始。
オフラインキャッシュ、共有機能、ダークモード、文字サイズ対応を実装。
READMEに起動手順・テスト手順・アーキテクチャ方針を記載。
```

### B. API実装

```text
TypeScript + Fastify + Prisma + PostgreSQL のBFFを実装。
エンドポイント: /articles /search /bookmarks /devices /auth。
認証はOAuth2(PKCE)またはメールOTP。
レート制限・入力バリデーション・テストを必須とする。
```

### C. UIコンポーネント設計

```text
テーマトークン(light/dark)、文字サイズスケール、ArticleCard・TagChip・EmptyState等
再利用コンポーネントを app/src/components に実装。
主要コンポーネントのスナップショットテストを追加。
```

## ロードマップ

### MVP（6〜10週間）
- 情報設計
- RNプロトタイプ
- 最小BFF
- ブックマーク/通知設定
- QA/ストア準備

### v1（+6〜10週間）
- OAuth2+PKCE
- 通知配信運用
- 検索改善
- 広告/PR表示ルール整備

### 将来
- コメント（通報/モデレーション）
- サブスク/IAP
- ウォレット連携（閲覧→署名）

## 参考: 機能優先度

| 機能 | 優先度 |
|---|---|
| 記事一覧/詳細 | 必須 |
| カテゴリ/タグ | 必須 |
| ブックマーク | 必須 |
| 検索 | 推奨 |
| 通知 | 推奨 |
| ログイン | 推奨 |
| コメント | 将来 |
| 課金/IAP | 将来 |
| ウォレット連携 | 将来 |

---

このレポートは、nft-labo.tokyo の「コンテンツ資産を活かした段階的アプリ化」を前提に、まずはニュースアプリとしての体験価値（読む・探す・保存・通知）を最短で提供し、その後にコミュニティ/収益化/Web3機能を安全に追加するための実装計画として利用できる。
