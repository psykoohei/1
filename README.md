# 連想スプリント（MVP）

待ち時間向けの「連想ゲーム強め」ミニWebアプリです。

## 実装した要件
- 7〜30秒の短尺モード（3連想/ブリッジ/タブー/カテゴリ/選択式）
- 自動で次に進まない（`もう1本`でのみ開始）
- `中断`ボタン1発で終了、結果表示なし
- 30秒ごとの「顔上げ」通知（vibrate + alert）
- `列モード`で入力型モードを封印
- `あとで箱`で気になるお題のみ保存

## 起動
```bash
python3 -m http.server 4173
```
ブラウザで `http://localhost:4173` を開いてください。
